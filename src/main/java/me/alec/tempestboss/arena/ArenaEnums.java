package me.alec.tempestboss.arena;

public class ArenaEnums {
    public enum TriggerMode { MANUAL, ENTER }
    public enum IntroMode { INHERIT, ON, OFF }
    public enum LootDelivery { INHERIT, NONE, CHEST, DROP, PER_PLAYER }
}
